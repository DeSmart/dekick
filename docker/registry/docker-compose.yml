version: "3.9"
services:
  registry:
    restart: always
    image: registry:2
    ports:
      - 5000:5000
    volumes:
      - ${DEKICK_PATH}/docker/registry/config.yml:/etc/docker/registry/config.yml
      - ${DEKICK_PATH}/tmp/docker-registry-data/:/var/lib/registry
